@model IEnumerable<AppTask.Models.Incidente>

@{
    ViewData["Title"] = "Incidentes";
}

<div class="page-header">

    <div>

        <h1 class="page-title">
            Incidentes
        </h1>

        <p class="page-subtitle">
            @Model.Count() incidente(s) cadastrado(s)
        </p>

    </div>

    <a href="/Incidente/Create"
       class="btn-accent">
        ➕ Novo Incidente
    </a>

</div>


@if (!Model.Any())
{
    <div class="empty-state">

        <p>
            ⚠️ Nenhum incidente cadastrado ainda.
        </p>

        <a href="/Incidente/Create"
           class="btn-accent">
            ➕ Registrar primeiro incidente
        </a>

    </div>
}
else
{
    <div class="card-grid">

        @foreach (var item in Model)
        {
            <div class="entity-card status-warn">

                <div class="entity-card-header">

                    <h3 class="entity-card-title">
                        @item.DescricaoProblema
                    </h3>

                    <span class="status-badge status-warn">
                        ⚠️ Incidente
                    </span>

                </div>


                <div class="entity-owner">

                    <span class="avatar-badge">
                        ⚠️
                    </span>

                    <span class="entity-owner-name">
                        Incidente registrado
                    </span>

                </div>


                <div class="meta-list">

                    <div class="meta-row">
                        📅 Data:
                        <strong>
                            @item.DataIncidente
                        </strong>
                    </div>

                    <div class="meta-row">
                        ⚠️ Problema:
                        <strong>
                            @item.DescricaoProblema
                        </strong>
                    </div>

                    <div class="meta-row">
                        💡 Solução:
                        <strong>
                            @(string.IsNullOrWhiteSpace(item.Solucao)
                                                ? "—"
                                                : item.Solucao)
                </strong>
            </div>

                    <div class="meta-row">
                        ✅ Resolvido:
                        <strong>
                            @item.Resolvido
                        </strong>
                    </div>

                </div>


                <div class="entity-card-actions">

                    <a href="/Incidente/Details/@item.Codigo"
                       class="btn-pill btn-pill-ghost">
                        Abrir
                    </a>

                    <a href="/Incidente/Edit/@item.Codigo"
                       class="btn-pill btn-pill-accent">
                        Editar
                    </a>

                    <button type="button"
                            class="btn-pill btn-pill-danger"
                            data-bs-toggle="modal"
                            data-bs-target="#deleteModal"
                            data-id="@item.Codigo"
                            data-descricao="@item.DescricaoProblema">
                        Excluir
                    </button>

                </div>

            </div>
        }

    </div>
}


<!-- MODAL DE EXCLUSÃO -->

<div class="modal fade"
     id="deleteModal"
     tabindex="-1"
     aria-labelledby="deleteModalLabel"
     aria-hidden="true">

    <div class="modal-dialog">

        <div class="modal-content">

            <div class="modal-header bg-danger text-white">

                <h5 class="modal-title"
                    id="deleteModalLabel">
                    Confirmação de Exclusão
                </h5>

                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="modal"
                        aria-label="Close">
                </button>

            </div>


            <div class="modal-body">

                Deseja realmente excluir o incidente:

                <strong id="incidenteDescricao"></strong>?

            </div>


            <div class="modal-footer">

                <button type="button"
                        class="modal-btn-cancel btn btn-secondary"
                        data-bs-dismiss="modal">
                    Cancelar
                </button>

                <form id="deleteForm"
                      method="post"
                      action="">

                    @Html.AntiForgeryToken()

                    <button type="submit"
                            class="btn btn-danger">
                        Sim, Excluir
                    </button>

                </form>

            </div>

        </div>

    </div>

</div>


@section Scripts {

    <script>

        var deleteModal =
            document.getElementById('deleteModal');

        deleteModal.addEventListener(
            'show.bs.modal',
            function (event) {

                var button =
                    event.relatedTarget;

                var id =
                    button.getAttribute('data-id');

                var descricao =
                    button.getAttribute(
                        'data-descricao'
                    );

                var modalDescricaoSpan =
                    deleteModal.querySelector(
                        '#incidenteDescricao'
                    );

                modalDescricaoSpan.textContent =
                    descricao;

                var deleteForm =
                    deleteModal.querySelector(
                        '#deleteForm'
                    );

                deleteForm.action =
                    '/Incidente/Delete/' + id;
            }
        );

    </script>
}
